import logo from '../assets/images/logo.svg';

const Layout = () => {
  return (
    <div>
      <div>
        <img src={logo} alt="Logo Image" />
      </div>
    </div>
  )
}

export default Layout
